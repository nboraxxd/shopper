const PRODUCTS = '/products'

export const PATH = {
  HOMEPAGE: '/',
  PRODUCTS: PRODUCTS,
  productDetail: PRODUCTS + '/:slug',
  CATEGORY: '/:slug/:id',
  LOGIN: '/login',
  REGISTER: '/register',
  PROFILE: '/profile',
}
